"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OperationTokenType = void 0;
exports.generateWorkflowRunOperationToken = generateWorkflowRunOperationToken;
exports.generateUpdateWorkflowOperationToken = generateUpdateWorkflowOperationToken;
exports.generateActivityOperationToken = generateActivityOperationToken;
exports.encodeOperationToken = encodeOperationToken;
exports.loadOperationToken = loadOperationToken;
exports.loadWorkflowRunOperationToken = loadWorkflowRunOperationToken;
exports.assertWorkflowRunOperationToken = assertWorkflowRunOperationToken;
exports.loadUpdateWorkflowOperationToken = loadUpdateWorkflowOperationToken;
exports.assertUpdateWorkflowOperationToken = assertUpdateWorkflowOperationToken;
exports.assertActivityOperationToken = assertActivityOperationToken;
exports.base64URLEncodeNoPadding = base64URLEncodeNoPadding;
/**
 * Known, currently-supported Operation token types.
 *
 * @internal
 * @hidden
 */
exports.OperationTokenType = {
    WORKFLOW_RUN: 1,
    ACTIVITY: 2,
    UPDATE_WORKFLOW: 3,
};
/**
 * Generate a workflow run Operation token.
 */
function generateWorkflowRunOperationToken(namespace, workflowId) {
    const token = {
        t: exports.OperationTokenType.WORKFLOW_RUN,
        ns: namespace,
        wid: workflowId,
    };
    return encodeOperationToken(token);
}
/**
 * Generate an update workflow Operation token.
 *
 * @throws {TypeError} if `namespace`, `workflowId`, or `updateId` is empty.
 */
function generateUpdateWorkflowOperationToken(namespace, workflowId, runId, updateId) {
    if (!namespace) {
        throw new TypeError('invalid update workflow token: missing namespace');
    }
    if (!workflowId) {
        throw new TypeError('invalid update workflow token: missing workflow ID (wid)');
    }
    if (!updateId) {
        throw new TypeError('invalid update workflow token: missing update ID (uid)');
    }
    const token = {
        t: exports.OperationTokenType.UPDATE_WORKFLOW,
        ns: namespace,
        wid: workflowId,
        // `rid` is optional, so omit it if not present
        rid: runId ? runId : undefined,
        uid: updateId,
    };
    return base64URLEncodeNoPadding(JSON.stringify(token));
}
/**
 * Generate an activity Operation token.
 */
function generateActivityOperationToken(namespace, activityId, runId) {
    const token = {
        t: exports.OperationTokenType.ACTIVITY,
        ns: namespace,
        aid: activityId,
        rid: runId,
    };
    return encodeOperationToken(token);
}
/**
 * Encode an OperationToken as a string.
 */
function encodeOperationToken(token) {
    return base64URLEncodeNoPadding(JSON.stringify(token));
}
/**
 * Load and validate the common fields of an Operation token.
 */
function loadOperationToken(data) {
    if (!data) {
        throw new TypeError('invalid operation token: token is empty');
    }
    let decoded;
    try {
        decoded = base64URLDecodeNoPadding(data);
    }
    catch (err) {
        throw new TypeError('failed to decode token', { cause: err });
    }
    let token;
    try {
        token = JSON.parse(decoded);
    }
    catch (err) {
        throw new TypeError('failed to unmarshal Operation token', { cause: err });
    }
    if (typeof token !== 'object' || token == null) {
        throw new TypeError(`invalid operation token: expected object, got ${typeof token}`);
    }
    if (token.v !== undefined && token.v !== 0) {
        throw new TypeError('invalid operation token: "v" field should not be present');
    }
    if (typeof token.t !== 'number') {
        throw new TypeError(`invalid operation token: expected token type to be a number, got ${typeof token.t}`);
    }
    if (!isOperationTokenType(token.t)) {
        throw new TypeError(`invalid operation token: unknown token type: ${token.t}`);
    }
    if (typeof token.ns !== 'string') {
        throw new TypeError(`invalid operation token: expected namespace to be a string, got ${typeof token.ns}`);
    }
    return token;
}
/**
 * Load and validate a workflow run Operation token.
 */
function loadWorkflowRunOperationToken(data) {
    const token = loadOperationToken(data);
    assertWorkflowRunOperationToken(token);
    return token;
}
/**
 * Assert that an OperationToken identifies a workflow run.
 */
function assertWorkflowRunOperationToken(token) {
    if (token.t !== exports.OperationTokenType.WORKFLOW_RUN) {
        throw new TypeError(`invalid workflow token type: ${token.t}, expected: ${exports.OperationTokenType.WORKFLOW_RUN}`);
    }
    if (!token.wid || typeof token.wid !== 'string') {
        throw new TypeError('invalid workflow run token: missing workflow ID (wid)');
    }
}
/**
 * Load and validate an update workflow Operation token.
 */
function loadUpdateWorkflowOperationToken(data) {
    const token = loadOperationToken(data);
    assertUpdateWorkflowOperationToken(token);
    return token;
}
/**
 * Assert that an OperationToken identifies an update workflow.
 */
function assertUpdateWorkflowOperationToken(token) {
    if (token.t !== exports.OperationTokenType.UPDATE_WORKFLOW) {
        throw new TypeError(`invalid update workflow token type: ${token.t}, expected: ${exports.OperationTokenType.UPDATE_WORKFLOW}`);
    }
    if (!token.wid || typeof token.wid !== 'string') {
        throw new TypeError('invalid update workflow token: missing workflow ID (wid)');
    }
    const uid = token.uid;
    if (!uid || typeof uid !== 'string') {
        throw new TypeError('invalid update workflow token: missing update ID (uid)');
    }
}
/**
 * Assert that an OperationToken identifies an activity.
 */
function assertActivityOperationToken(token) {
    if (token.t !== exports.OperationTokenType.ACTIVITY) {
        throw new TypeError(`invalid activity token type: ${token.t}, expected: ${exports.OperationTokenType.ACTIVITY}`);
    }
    if (!token.aid || typeof token.aid !== 'string') {
        throw new TypeError('invalid activity token: missing or invalid activity ID (aid)');
    }
    if (!token.rid || typeof token.rid !== 'string') {
        throw new TypeError('invalid activity token: missing or invalid activity run ID (rid)');
    }
}
function isOperationTokenType(value) {
    return Object.values(exports.OperationTokenType).includes(value);
}
// Exported for use in tests.
function base64URLEncodeNoPadding(str) {
    const base64 = Buffer.from(str).toString('base64url');
    return base64.replace(/[=]+$/, '');
}
function base64URLDecodeNoPadding(str) {
    // Validate the string contains only valid base64URL characters
    if (!/^[A-Za-z0-9_-]*$/.test(str)) {
        throw new TypeError('invalid base64URL encoded string: contains invalid characters');
    }
    const paddingLength = str.length % 4;
    if (paddingLength > 0) {
        str += '='.repeat(4 - paddingLength);
    }
    return Buffer.from(str, 'base64url').toString('utf-8');
}
//# sourceMappingURL=token.js.map