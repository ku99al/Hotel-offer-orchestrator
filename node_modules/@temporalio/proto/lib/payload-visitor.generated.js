"use strict";
// Code generated by packages/proto/scripts/gen-payload-visitor.ts. DO NOT EDIT.
Object.defineProperty(exports, "__esModule", { value: true });
exports.walkActivityExecutionResult = walkActivityExecutionResult;
exports.walkActivityResolution = walkActivityResolution;
exports.walkCoresdkActivityResultCancellation = walkCoresdkActivityResultCancellation;
exports.walkCoresdkActivityResultFailure = walkCoresdkActivityResultFailure;
exports.walkCoresdkActivityResultSuccess = walkCoresdkActivityResultSuccess;
exports.walkActivityTask = walkActivityTask;
exports.walkStart = walkStart;
exports.walkActivityHeartbeat = walkActivityHeartbeat;
exports.walkActivityTaskCompletion = walkActivityTaskCompletion;
exports.walkCoresdkChildWorkflowCancellation = walkCoresdkChildWorkflowCancellation;
exports.walkChildWorkflowResult = walkChildWorkflowResult;
exports.walkCoresdkChildWorkflowFailure = walkCoresdkChildWorkflowFailure;
exports.walkCoresdkChildWorkflowSuccess = walkCoresdkChildWorkflowSuccess;
exports.walkNexusOperationResult = walkNexusOperationResult;
exports.walkNexusTask = walkNexusTask;
exports.walkNexusTaskCompletion = walkNexusTaskCompletion;
exports.walkDoUpdate = walkDoUpdate;
exports.walkInitializeWorkflow = walkInitializeWorkflow;
exports.walkQueryWorkflow = walkQueryWorkflow;
exports.walkResolveActivity = walkResolveActivity;
exports.walkResolveChildWorkflowExecution = walkResolveChildWorkflowExecution;
exports.walkResolveChildWorkflowExecutionStart = walkResolveChildWorkflowExecutionStart;
exports.walkResolveChildWorkflowExecutionStartCancelled = walkResolveChildWorkflowExecutionStartCancelled;
exports.walkResolveNexusOperation = walkResolveNexusOperation;
exports.walkResolveNexusOperationStart = walkResolveNexusOperationStart;
exports.walkResolveRequestCancelExternalWorkflow = walkResolveRequestCancelExternalWorkflow;
exports.walkResolveSignalExternalWorkflow = walkResolveSignalExternalWorkflow;
exports.walkSignalWorkflow = walkSignalWorkflow;
exports.walkWorkflowActivation = walkWorkflowActivation;
exports.walkWorkflowActivationJob = walkWorkflowActivationJob;
exports.walkCancelWorkflowExecution = walkCancelWorkflowExecution;
exports.walkCompleteWorkflowExecution = walkCompleteWorkflowExecution;
exports.walkContinueAsNewWorkflowExecution = walkContinueAsNewWorkflowExecution;
exports.walkFailWorkflowExecution = walkFailWorkflowExecution;
exports.walkModifyWorkflowProperties = walkModifyWorkflowProperties;
exports.walkQueryResult = walkQueryResult;
exports.walkQuerySuccess = walkQuerySuccess;
exports.walkScheduleActivity = walkScheduleActivity;
exports.walkScheduleLocalActivity = walkScheduleLocalActivity;
exports.walkScheduleNexusOperation = walkScheduleNexusOperation;
exports.walkSignalExternalWorkflowExecution = walkSignalExternalWorkflowExecution;
exports.walkStartChildWorkflowExecution = walkStartChildWorkflowExecution;
exports.walkUpdateResponse = walkUpdateResponse;
exports.walkUpsertWorkflowSearchAttributes = walkUpsertWorkflowSearchAttributes;
exports.walkWorkflowCommand = walkWorkflowCommand;
exports.walkCoresdkWorkflowCompletionFailure = walkCoresdkWorkflowCompletionFailure;
exports.walkCoresdkWorkflowCompletionSuccess = walkCoresdkWorkflowCompletionSuccess;
exports.walkWorkflowActivationCompletion = walkWorkflowActivationCompletion;
exports.walkCountActivityExecutionsResponse = walkCountActivityExecutionsResponse;
exports.walkTemporalApiWorkflowserviceV1CountActivityExecutionsResponseAggregationGroup = walkTemporalApiWorkflowserviceV1CountActivityExecutionsResponseAggregationGroup;
exports.walkCountNexusOperationExecutionsResponse = walkCountNexusOperationExecutionsResponse;
exports.walkTemporalApiWorkflowserviceV1CountNexusOperationExecutionsResponseAggregationGroup = walkTemporalApiWorkflowserviceV1CountNexusOperationExecutionsResponseAggregationGroup;
exports.walkCountSchedulesResponse = walkCountSchedulesResponse;
exports.walkTemporalApiWorkflowserviceV1CountSchedulesResponseAggregationGroup = walkTemporalApiWorkflowserviceV1CountSchedulesResponseAggregationGroup;
exports.walkCountWorkflowExecutionsResponse = walkCountWorkflowExecutionsResponse;
exports.walkTemporalApiWorkflowserviceV1CountWorkflowExecutionsResponseAggregationGroup = walkTemporalApiWorkflowserviceV1CountWorkflowExecutionsResponseAggregationGroup;
exports.walkCreateScheduleRequest = walkCreateScheduleRequest;
exports.walkCreateWorkerDeploymentVersionRequest = walkCreateWorkerDeploymentVersionRequest;
exports.walkDescribeActivityExecutionResponse = walkDescribeActivityExecutionResponse;
exports.walkDescribeDeploymentResponse = walkDescribeDeploymentResponse;
exports.walkDescribeNexusOperationExecutionResponse = walkDescribeNexusOperationExecutionResponse;
exports.walkDescribeScheduleResponse = walkDescribeScheduleResponse;
exports.walkDescribeWorkerDeploymentVersionResponse = walkDescribeWorkerDeploymentVersionResponse;
exports.walkDescribeWorkflowExecutionResponse = walkDescribeWorkflowExecutionResponse;
exports.walkExecuteMultiOperationRequest = walkExecuteMultiOperationRequest;
exports.walkOperation = walkOperation;
exports.walkExecuteMultiOperationResponse = walkExecuteMultiOperationResponse;
exports.walkResponse = walkResponse;
exports.walkGetCurrentDeploymentResponse = walkGetCurrentDeploymentResponse;
exports.walkGetDeploymentReachabilityResponse = walkGetDeploymentReachabilityResponse;
exports.walkGetWorkflowExecutionHistoryResponse = walkGetWorkflowExecutionHistoryResponse;
exports.walkGetWorkflowExecutionHistoryReverseResponse = walkGetWorkflowExecutionHistoryReverseResponse;
exports.walkListActivityExecutionsResponse = walkListActivityExecutionsResponse;
exports.walkListArchivedWorkflowExecutionsResponse = walkListArchivedWorkflowExecutionsResponse;
exports.walkListClosedWorkflowExecutionsResponse = walkListClosedWorkflowExecutionsResponse;
exports.walkListNexusOperationExecutionsResponse = walkListNexusOperationExecutionsResponse;
exports.walkListOpenWorkflowExecutionsResponse = walkListOpenWorkflowExecutionsResponse;
exports.walkListSchedulesResponse = walkListSchedulesResponse;
exports.walkListWorkflowExecutionsResponse = walkListWorkflowExecutionsResponse;
exports.walkPollActivityExecutionResponse = walkPollActivityExecutionResponse;
exports.walkPollActivityTaskQueueResponse = walkPollActivityTaskQueueResponse;
exports.walkPollNexusOperationExecutionResponse = walkPollNexusOperationExecutionResponse;
exports.walkPollNexusTaskQueueResponse = walkPollNexusTaskQueueResponse;
exports.walkPollWorkflowExecutionUpdateResponse = walkPollWorkflowExecutionUpdateResponse;
exports.walkPollWorkflowTaskQueueResponse = walkPollWorkflowTaskQueueResponse;
exports.walkQueryWorkflowRequest = walkQueryWorkflowRequest;
exports.walkQueryWorkflowResponse = walkQueryWorkflowResponse;
exports.walkRecordActivityTaskHeartbeatByIdRequest = walkRecordActivityTaskHeartbeatByIdRequest;
exports.walkRecordActivityTaskHeartbeatRequest = walkRecordActivityTaskHeartbeatRequest;
exports.walkResetWorkflowExecutionRequest = walkResetWorkflowExecutionRequest;
exports.walkRespondActivityTaskCanceledByIdRequest = walkRespondActivityTaskCanceledByIdRequest;
exports.walkRespondActivityTaskCanceledRequest = walkRespondActivityTaskCanceledRequest;
exports.walkRespondActivityTaskCompletedByIdRequest = walkRespondActivityTaskCompletedByIdRequest;
exports.walkRespondActivityTaskCompletedRequest = walkRespondActivityTaskCompletedRequest;
exports.walkRespondActivityTaskFailedByIdRequest = walkRespondActivityTaskFailedByIdRequest;
exports.walkRespondActivityTaskFailedByIdResponse = walkRespondActivityTaskFailedByIdResponse;
exports.walkRespondActivityTaskFailedRequest = walkRespondActivityTaskFailedRequest;
exports.walkRespondActivityTaskFailedResponse = walkRespondActivityTaskFailedResponse;
exports.walkRespondNexusTaskCompletedRequest = walkRespondNexusTaskCompletedRequest;
exports.walkRespondNexusTaskFailedRequest = walkRespondNexusTaskFailedRequest;
exports.walkRespondQueryTaskCompletedRequest = walkRespondQueryTaskCompletedRequest;
exports.walkRespondWorkflowTaskCompletedRequest = walkRespondWorkflowTaskCompletedRequest;
exports.walkRespondWorkflowTaskCompletedResponse = walkRespondWorkflowTaskCompletedResponse;
exports.walkRespondWorkflowTaskFailedRequest = walkRespondWorkflowTaskFailedRequest;
exports.walkScanWorkflowExecutionsResponse = walkScanWorkflowExecutionsResponse;
exports.walkSetCurrentDeploymentRequest = walkSetCurrentDeploymentRequest;
exports.walkSetCurrentDeploymentResponse = walkSetCurrentDeploymentResponse;
exports.walkSignalWithStartWorkflowExecutionRequest = walkSignalWithStartWorkflowExecutionRequest;
exports.walkSignalWorkflowExecutionRequest = walkSignalWorkflowExecutionRequest;
exports.walkStartActivityExecutionRequest = walkStartActivityExecutionRequest;
exports.walkStartBatchOperationRequest = walkStartBatchOperationRequest;
exports.walkStartNexusOperationExecutionRequest = walkStartNexusOperationExecutionRequest;
exports.walkStartWorkflowExecutionRequest = walkStartWorkflowExecutionRequest;
exports.walkStartWorkflowExecutionResponse = walkStartWorkflowExecutionResponse;
exports.walkTerminateWorkflowExecutionRequest = walkTerminateWorkflowExecutionRequest;
exports.walkUpdateScheduleRequest = walkUpdateScheduleRequest;
exports.walkUpdateWorkerDeploymentVersionComputeConfigRequest = walkUpdateWorkerDeploymentVersionComputeConfigRequest;
exports.walkUpdateWorkerDeploymentVersionMetadataRequest = walkUpdateWorkerDeploymentVersionMetadataRequest;
exports.walkUpdateWorkerDeploymentVersionMetadataResponse = walkUpdateWorkerDeploymentVersionMetadataResponse;
exports.walkUpdateWorkflowExecutionRequest = walkUpdateWorkflowExecutionRequest;
exports.walkUpdateWorkflowExecutionResponse = walkUpdateWorkflowExecutionResponse;
exports.walkValidateWorkerDeploymentVersionComputeConfigRequest = walkValidateWorkerDeploymentVersionComputeConfigRequest;
function walkActivityExecutionResult(root, env, context) {
    const pending = [];
    walk_coresdk_activity_result_ActivityExecutionResult(root, env, context, pending);
    return pending;
}
function walkActivityResolution(root, env, context) {
    const pending = [];
    walk_coresdk_activity_result_ActivityResolution(root, env, context, pending);
    return pending;
}
function walkCoresdkActivityResultCancellation(root, env, context) {
    const pending = [];
    walk_coresdk_activity_result_Cancellation(root, env, context, pending);
    return pending;
}
function walkCoresdkActivityResultFailure(root, env, context) {
    const pending = [];
    walk_coresdk_activity_result_Failure(root, env, context, pending);
    return pending;
}
function walkCoresdkActivityResultSuccess(root, env, context) {
    const pending = [];
    walk_coresdk_activity_result_Success(root, env, context, pending);
    return pending;
}
function walkActivityTask(root, env, context) {
    const pending = [];
    walk_coresdk_activity_task_ActivityTask(root, env, context, pending);
    return pending;
}
function walkStart(root, env, context) {
    const pending = [];
    walk_coresdk_activity_task_Start(root, env, context, pending);
    return pending;
}
function walkActivityHeartbeat(root, env, context) {
    const pending = [];
    walk_coresdk_ActivityHeartbeat(root, env, context, pending);
    return pending;
}
function walkActivityTaskCompletion(root, env, context) {
    const pending = [];
    walk_coresdk_ActivityTaskCompletion(root, env, context, pending);
    return pending;
}
function walkCoresdkChildWorkflowCancellation(root, env, context) {
    const pending = [];
    walk_coresdk_child_workflow_Cancellation(root, env, context, pending);
    return pending;
}
function walkChildWorkflowResult(root, env, context) {
    const pending = [];
    walk_coresdk_child_workflow_ChildWorkflowResult(root, env, context, pending);
    return pending;
}
function walkCoresdkChildWorkflowFailure(root, env, context) {
    const pending = [];
    walk_coresdk_child_workflow_Failure(root, env, context, pending);
    return pending;
}
function walkCoresdkChildWorkflowSuccess(root, env, context) {
    const pending = [];
    walk_coresdk_child_workflow_Success(root, env, context, pending);
    return pending;
}
function walkNexusOperationResult(root, env, context) {
    const pending = [];
    walk_coresdk_nexus_NexusOperationResult(root, env, context, pending);
    return pending;
}
function walkNexusTask(root, env, context) {
    const pending = [];
    walk_coresdk_nexus_NexusTask(root, env, context, pending);
    return pending;
}
function walkNexusTaskCompletion(root, env, context) {
    const pending = [];
    walk_coresdk_nexus_NexusTaskCompletion(root, env, context, pending);
    return pending;
}
function walkDoUpdate(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_DoUpdate(root, env, context, pending);
    return pending;
}
function walkInitializeWorkflow(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_InitializeWorkflow(root, env, context, pending);
    return pending;
}
function walkQueryWorkflow(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_QueryWorkflow(root, env, context, pending);
    return pending;
}
function walkResolveActivity(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveActivity(root, env, context, pending);
    return pending;
}
function walkResolveChildWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveChildWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkResolveChildWorkflowExecutionStart(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveChildWorkflowExecutionStart(root, env, context, pending);
    return pending;
}
function walkResolveChildWorkflowExecutionStartCancelled(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveChildWorkflowExecutionStartCancelled(root, env, context, pending);
    return pending;
}
function walkResolveNexusOperation(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveNexusOperation(root, env, context, pending);
    return pending;
}
function walkResolveNexusOperationStart(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveNexusOperationStart(root, env, context, pending);
    return pending;
}
function walkResolveRequestCancelExternalWorkflow(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveRequestCancelExternalWorkflow(root, env, context, pending);
    return pending;
}
function walkResolveSignalExternalWorkflow(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_ResolveSignalExternalWorkflow(root, env, context, pending);
    return pending;
}
function walkSignalWorkflow(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_SignalWorkflow(root, env, context, pending);
    return pending;
}
function walkWorkflowActivation(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_WorkflowActivation(root, env, context, pending);
    return pending;
}
function walkWorkflowActivationJob(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_activation_WorkflowActivationJob(root, env, context, pending);
    return pending;
}
function walkCancelWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_CancelWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkCompleteWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_CompleteWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkContinueAsNewWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_ContinueAsNewWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkFailWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_FailWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkModifyWorkflowProperties(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_ModifyWorkflowProperties(root, env, context, pending);
    return pending;
}
function walkQueryResult(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_QueryResult(root, env, context, pending);
    return pending;
}
function walkQuerySuccess(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_QuerySuccess(root, env, context, pending);
    return pending;
}
function walkScheduleActivity(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_ScheduleActivity(root, env, context, pending);
    return pending;
}
function walkScheduleLocalActivity(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_ScheduleLocalActivity(root, env, context, pending);
    return pending;
}
function walkScheduleNexusOperation(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_ScheduleNexusOperation(root, env, context, pending);
    return pending;
}
function walkSignalExternalWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_SignalExternalWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkStartChildWorkflowExecution(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_StartChildWorkflowExecution(root, env, context, pending);
    return pending;
}
function walkUpdateResponse(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_UpdateResponse(root, env, context, pending);
    return pending;
}
function walkUpsertWorkflowSearchAttributes(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_UpsertWorkflowSearchAttributes(root, env, context, pending);
    return pending;
}
function walkWorkflowCommand(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_commands_WorkflowCommand(root, env, context, pending);
    return pending;
}
function walkCoresdkWorkflowCompletionFailure(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_completion_Failure(root, env, context, pending);
    return pending;
}
function walkCoresdkWorkflowCompletionSuccess(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_completion_Success(root, env, context, pending);
    return pending;
}
function walkWorkflowActivationCompletion(root, env, context) {
    const pending = [];
    walk_coresdk_workflow_completion_WorkflowActivationCompletion(root, env, context, pending);
    return pending;
}
function walkCountActivityExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountActivityExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkTemporalApiWorkflowserviceV1CountActivityExecutionsResponseAggregationGroup(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountActivityExecutionsResponse_AggregationGroup(root, env, context, pending);
    return pending;
}
function walkCountNexusOperationExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountNexusOperationExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkTemporalApiWorkflowserviceV1CountNexusOperationExecutionsResponseAggregationGroup(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountNexusOperationExecutionsResponse_AggregationGroup(root, env, context, pending);
    return pending;
}
function walkCountSchedulesResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountSchedulesResponse(root, env, context, pending);
    return pending;
}
function walkTemporalApiWorkflowserviceV1CountSchedulesResponseAggregationGroup(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountSchedulesResponse_AggregationGroup(root, env, context, pending);
    return pending;
}
function walkCountWorkflowExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountWorkflowExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkTemporalApiWorkflowserviceV1CountWorkflowExecutionsResponseAggregationGroup(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CountWorkflowExecutionsResponse_AggregationGroup(root, env, context, pending);
    return pending;
}
function walkCreateScheduleRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CreateScheduleRequest(root, env, context, pending);
    return pending;
}
function walkCreateWorkerDeploymentVersionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_CreateWorkerDeploymentVersionRequest(root, env, context, pending);
    return pending;
}
function walkDescribeActivityExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_DescribeActivityExecutionResponse(root, env, context, pending);
    return pending;
}
function walkDescribeDeploymentResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_DescribeDeploymentResponse(root, env, context, pending);
    return pending;
}
function walkDescribeNexusOperationExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_DescribeNexusOperationExecutionResponse(root, env, context, pending);
    return pending;
}
function walkDescribeScheduleResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_DescribeScheduleResponse(root, env, context, pending);
    return pending;
}
function walkDescribeWorkerDeploymentVersionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_DescribeWorkerDeploymentVersionResponse(root, env, context, pending);
    return pending;
}
function walkDescribeWorkflowExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_DescribeWorkflowExecutionResponse(root, env, context, pending);
    return pending;
}
function walkExecuteMultiOperationRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ExecuteMultiOperationRequest(root, env, context, pending);
    return pending;
}
function walkOperation(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ExecuteMultiOperationRequest_Operation(root, env, context, pending);
    return pending;
}
function walkExecuteMultiOperationResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ExecuteMultiOperationResponse(root, env, context, pending);
    return pending;
}
function walkResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ExecuteMultiOperationResponse_Response(root, env, context, pending);
    return pending;
}
function walkGetCurrentDeploymentResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_GetCurrentDeploymentResponse(root, env, context, pending);
    return pending;
}
function walkGetDeploymentReachabilityResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_GetDeploymentReachabilityResponse(root, env, context, pending);
    return pending;
}
function walkGetWorkflowExecutionHistoryResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_GetWorkflowExecutionHistoryResponse(root, env, context, pending);
    return pending;
}
function walkGetWorkflowExecutionHistoryReverseResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_GetWorkflowExecutionHistoryReverseResponse(root, env, context, pending);
    return pending;
}
function walkListActivityExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListActivityExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkListArchivedWorkflowExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListArchivedWorkflowExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkListClosedWorkflowExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListClosedWorkflowExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkListNexusOperationExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListNexusOperationExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkListOpenWorkflowExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListOpenWorkflowExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkListSchedulesResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListSchedulesResponse(root, env, context, pending);
    return pending;
}
function walkListWorkflowExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ListWorkflowExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkPollActivityExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_PollActivityExecutionResponse(root, env, context, pending);
    return pending;
}
function walkPollActivityTaskQueueResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_PollActivityTaskQueueResponse(root, env, context, pending);
    return pending;
}
function walkPollNexusOperationExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_PollNexusOperationExecutionResponse(root, env, context, pending);
    return pending;
}
function walkPollNexusTaskQueueResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_PollNexusTaskQueueResponse(root, env, context, pending);
    return pending;
}
function walkPollWorkflowExecutionUpdateResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_PollWorkflowExecutionUpdateResponse(root, env, context, pending);
    return pending;
}
function walkPollWorkflowTaskQueueResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_PollWorkflowTaskQueueResponse(root, env, context, pending);
    return pending;
}
function walkQueryWorkflowRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_QueryWorkflowRequest(root, env, context, pending);
    return pending;
}
function walkQueryWorkflowResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_QueryWorkflowResponse(root, env, context, pending);
    return pending;
}
function walkRecordActivityTaskHeartbeatByIdRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RecordActivityTaskHeartbeatByIdRequest(root, env, context, pending);
    return pending;
}
function walkRecordActivityTaskHeartbeatRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RecordActivityTaskHeartbeatRequest(root, env, context, pending);
    return pending;
}
function walkResetWorkflowExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ResetWorkflowExecutionRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskCanceledByIdRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskCanceledByIdRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskCanceledRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskCanceledRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskCompletedByIdRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskCompletedByIdRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskCompletedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskCompletedRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskFailedByIdRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedByIdRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskFailedByIdResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedByIdResponse(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskFailedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedRequest(root, env, context, pending);
    return pending;
}
function walkRespondActivityTaskFailedResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedResponse(root, env, context, pending);
    return pending;
}
function walkRespondNexusTaskCompletedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondNexusTaskCompletedRequest(root, env, context, pending);
    return pending;
}
function walkRespondNexusTaskFailedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondNexusTaskFailedRequest(root, env, context, pending);
    return pending;
}
function walkRespondQueryTaskCompletedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondQueryTaskCompletedRequest(root, env, context, pending);
    return pending;
}
function walkRespondWorkflowTaskCompletedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondWorkflowTaskCompletedRequest(root, env, context, pending);
    return pending;
}
function walkRespondWorkflowTaskCompletedResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondWorkflowTaskCompletedResponse(root, env, context, pending);
    return pending;
}
function walkRespondWorkflowTaskFailedRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_RespondWorkflowTaskFailedRequest(root, env, context, pending);
    return pending;
}
function walkScanWorkflowExecutionsResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ScanWorkflowExecutionsResponse(root, env, context, pending);
    return pending;
}
function walkSetCurrentDeploymentRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_SetCurrentDeploymentRequest(root, env, context, pending);
    return pending;
}
function walkSetCurrentDeploymentResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_SetCurrentDeploymentResponse(root, env, context, pending);
    return pending;
}
function walkSignalWithStartWorkflowExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_SignalWithStartWorkflowExecutionRequest(root, env, context, pending);
    return pending;
}
function walkSignalWorkflowExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_SignalWorkflowExecutionRequest(root, env, context, pending);
    return pending;
}
function walkStartActivityExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_StartActivityExecutionRequest(root, env, context, pending);
    return pending;
}
function walkStartBatchOperationRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_StartBatchOperationRequest(root, env, context, pending);
    return pending;
}
function walkStartNexusOperationExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_StartNexusOperationExecutionRequest(root, env, context, pending);
    return pending;
}
function walkStartWorkflowExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_StartWorkflowExecutionRequest(root, env, context, pending);
    return pending;
}
function walkStartWorkflowExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_StartWorkflowExecutionResponse(root, env, context, pending);
    return pending;
}
function walkTerminateWorkflowExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_TerminateWorkflowExecutionRequest(root, env, context, pending);
    return pending;
}
function walkUpdateScheduleRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_UpdateScheduleRequest(root, env, context, pending);
    return pending;
}
function walkUpdateWorkerDeploymentVersionComputeConfigRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_UpdateWorkerDeploymentVersionComputeConfigRequest(root, env, context, pending);
    return pending;
}
function walkUpdateWorkerDeploymentVersionMetadataRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_UpdateWorkerDeploymentVersionMetadataRequest(root, env, context, pending);
    return pending;
}
function walkUpdateWorkerDeploymentVersionMetadataResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_UpdateWorkerDeploymentVersionMetadataResponse(root, env, context, pending);
    return pending;
}
function walkUpdateWorkflowExecutionRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_UpdateWorkflowExecutionRequest(root, env, context, pending);
    return pending;
}
function walkUpdateWorkflowExecutionResponse(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_UpdateWorkflowExecutionResponse(root, env, context, pending);
    return pending;
}
function walkValidateWorkerDeploymentVersionComputeConfigRequest(root, env, context) {
    const pending = [];
    walk_temporal_api_workflowservice_v1_ValidateWorkerDeploymentVersionComputeConfigRequest(root, env, context, pending);
    return pending;
}
function walk_coresdk_activity_result_ActivityExecutionResult(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.activity_result.ActivityExecutionResult', context)
        : context;
    {
        const c = o.completed;
        if (c != null)
            walk_coresdk_activity_result_Success(c, env, ctx, pending);
    }
    {
        const c = o.failed;
        if (c != null)
            walk_coresdk_activity_result_Failure(c, env, ctx, pending);
    }
    {
        const c = o.cancelled;
        if (c != null)
            walk_coresdk_activity_result_Cancellation(c, env, ctx, pending);
    }
}
function walk_coresdk_activity_result_ActivityResolution(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.activity_result.ActivityResolution', context) : context;
    {
        const c = o.completed;
        if (c != null)
            walk_coresdk_activity_result_Success(c, env, ctx, pending);
    }
    {
        const c = o.failed;
        if (c != null)
            walk_coresdk_activity_result_Failure(c, env, ctx, pending);
    }
    {
        const c = o.cancelled;
        if (c != null)
            walk_coresdk_activity_result_Cancellation(c, env, ctx, pending);
    }
}
function walk_coresdk_activity_result_Cancellation(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.activity_result.Cancellation', context) : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_activity_result_Failure(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.activity_result.Failure', context) : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_activity_result_Success(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.activity_result.Success', context) : context;
    {
        const p = o.result;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.result = r;
            }));
    }
}
function walk_coresdk_activity_task_ActivityTask(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.activity_task.ActivityTask', context) : context;
    {
        const c = o.start;
        if (c != null)
            walk_coresdk_activity_task_Start(c, env, ctx, pending);
    }
}
function walk_coresdk_activity_task_Start(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.activity_task.Start', context) : context;
    {
        const m = o.headerFields;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
    {
        const a = o.input;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.input = r;
            }));
    }
    {
        const a = o.heartbeatDetails;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.heartbeatDetails = r;
            }));
    }
}
function walk_coresdk_ActivityHeartbeat(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.ActivityHeartbeat', context) : context;
    {
        const a = o.details;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.details = r;
            }));
    }
}
function walk_coresdk_ActivityTaskCompletion(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.ActivityTaskCompletion', context) : context;
    {
        const c = o.result;
        if (c != null)
            walk_coresdk_activity_result_ActivityExecutionResult(c, env, ctx, pending);
    }
}
function walk_coresdk_child_workflow_Cancellation(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.child_workflow.Cancellation', context) : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_child_workflow_ChildWorkflowResult(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.child_workflow.ChildWorkflowResult', context) : context;
    {
        const c = o.completed;
        if (c != null)
            walk_coresdk_child_workflow_Success(c, env, ctx, pending);
    }
    {
        const c = o.failed;
        if (c != null)
            walk_coresdk_child_workflow_Failure(c, env, ctx, pending);
    }
    {
        const c = o.cancelled;
        if (c != null)
            walk_coresdk_child_workflow_Cancellation(c, env, ctx, pending);
    }
}
function walk_coresdk_child_workflow_Failure(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.child_workflow.Failure', context) : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_child_workflow_Success(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.child_workflow.Success', context) : context;
    {
        const p = o.result;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.result = r;
            }));
    }
}
function walk_coresdk_nexus_NexusOperationResult(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.nexus.NexusOperationResult', context) : context;
    {
        const p = o.completed;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.completed = r;
            }));
    }
    {
        const c = o.failed;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.cancelled;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.timedOut;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_nexus_NexusTask(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.nexus.NexusTask', context) : context;
    {
        const c = o.task;
        if (c != null)
            walk_temporal_api_workflowservice_v1_PollNexusTaskQueueResponse(c, env, ctx, pending);
    }
}
function walk_coresdk_nexus_NexusTaskCompletion(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.nexus.NexusTaskCompletion', context) : context;
    {
        const c = o.completed;
        if (c != null)
            walk_temporal_api_nexus_v1_Response(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_DoUpdate(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_activation.DoUpdate', context) : context;
    {
        const a = o.input;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.input = r;
            }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
}
function walk_coresdk_workflow_activation_InitializeWorkflow(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.InitializeWorkflow', context)
        : context;
    {
        const a = o.arguments;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.arguments = r;
            }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
    {
        const c = o.continuedFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastCompletionResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_coresdk_workflow_activation_QueryWorkflow(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_activation.QueryWorkflow', context) : context;
    {
        const a = o.arguments;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.arguments = r;
            }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
}
function walk_coresdk_workflow_activation_ResolveActivity(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveActivity', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_coresdk_activity_result_ActivityResolution(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveChildWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveChildWorkflowExecution', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_coresdk_child_workflow_ChildWorkflowResult(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveChildWorkflowExecutionStart(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveChildWorkflowExecutionStart', context)
        : context;
    {
        const c = o.cancelled;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveChildWorkflowExecutionStartCancelled(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveChildWorkflowExecutionStartCancelled(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveChildWorkflowExecutionStartCancelled', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveNexusOperation(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveNexusOperation', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_coresdk_nexus_NexusOperationResult(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveNexusOperationStart(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveNexusOperationStart', context)
        : context;
    {
        const c = o.failed;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveRequestCancelExternalWorkflow(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveRequestCancelExternalWorkflow', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_ResolveSignalExternalWorkflow(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.ResolveSignalExternalWorkflow', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_SignalWorkflow(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_activation.SignalWorkflow', context) : context;
    {
        const a = o.input;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.input = r;
            }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
}
function walk_coresdk_workflow_activation_WorkflowActivation(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.WorkflowActivation', context)
        : context;
    {
        const a = o.jobs;
        if (a)
            for (const v of a)
                walk_coresdk_workflow_activation_WorkflowActivationJob(v, env, ctx, pending);
    }
}
function walk_coresdk_workflow_activation_WorkflowActivationJob(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_activation.WorkflowActivationJob', context)
        : context;
    {
        const c = o.initializeWorkflow;
        if (c != null)
            walk_coresdk_workflow_activation_InitializeWorkflow(c, env, ctx, pending);
    }
    {
        const c = o.queryWorkflow;
        if (c != null)
            walk_coresdk_workflow_activation_QueryWorkflow(c, env, ctx, pending);
    }
    {
        const c = o.signalWorkflow;
        if (c != null)
            walk_coresdk_workflow_activation_SignalWorkflow(c, env, ctx, pending);
    }
    {
        const c = o.resolveActivity;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveActivity(c, env, ctx, pending);
    }
    {
        const c = o.resolveChildWorkflowExecutionStart;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveChildWorkflowExecutionStart(c, env, ctx, pending);
    }
    {
        const c = o.resolveChildWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveChildWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.resolveSignalExternalWorkflow;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveSignalExternalWorkflow(c, env, ctx, pending);
    }
    {
        const c = o.resolveRequestCancelExternalWorkflow;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveRequestCancelExternalWorkflow(c, env, ctx, pending);
    }
    {
        const c = o.doUpdate;
        if (c != null)
            walk_coresdk_workflow_activation_DoUpdate(c, env, ctx, pending);
    }
    {
        const c = o.resolveNexusOperationStart;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveNexusOperationStart(c, env, ctx, pending);
    }
    {
        const c = o.resolveNexusOperation;
        if (c != null)
            walk_coresdk_workflow_activation_ResolveNexusOperation(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_commands_CancelWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.CancelWorkflowExecution', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_commands_CompleteWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.CompleteWorkflowExecution', context)
        : context;
    {
        const p = o.result;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.result = r;
            }));
    }
}
function walk_coresdk_workflow_commands_ContinueAsNewWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.ContinueAsNewWorkflowExecution', context)
        : context;
    {
        const a = o.arguments;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.arguments = r;
            }));
    }
    {
        const m = o.memo;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_coresdk_workflow_commands_FailWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.FailWorkflowExecution', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_commands_ModifyWorkflowProperties(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.ModifyWorkflowProperties', context)
        : context;
    {
        const c = o.upsertedMemo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_commands_QueryResult(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_commands.QueryResult', context) : context;
    {
        const c = o.succeeded;
        if (c != null)
            walk_coresdk_workflow_commands_QuerySuccess(c, env, ctx, pending);
    }
    {
        const c = o.failed;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_commands_QuerySuccess(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_commands.QuerySuccess', context) : context;
    {
        const p = o.response;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.response = r;
            }));
    }
}
function walk_coresdk_workflow_commands_ScheduleActivity(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_commands.ScheduleActivity', context) : context;
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
    {
        const a = o.arguments;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.arguments = r;
            }));
    }
}
function walk_coresdk_workflow_commands_ScheduleLocalActivity(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.ScheduleLocalActivity', context)
        : context;
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
    {
        const a = o.arguments;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.arguments = r;
            }));
    }
}
function walk_coresdk_workflow_commands_ScheduleNexusOperation(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.ScheduleNexusOperation', context)
        : context;
    {
        const p = o.input;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.input = r;
            }));
    }
}
function walk_coresdk_workflow_commands_SignalExternalWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.SignalExternalWorkflowExecution', context)
        : context;
    {
        const a = o.args;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.args = r;
            }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
}
function walk_coresdk_workflow_commands_StartChildWorkflowExecution(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.StartChildWorkflowExecution', context)
        : context;
    {
        const a = o.input;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.input = r;
            }));
    }
    {
        if (!env.skipHeaders) {
            const m = o.headers;
            if (m)
                for (const [k, v] of Object.entries(m))
                    pending.push(env.transformPayload(v, ctx).then((r) => {
                        m[k] = r;
                    }));
        }
    }
    {
        const m = o.memo;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_coresdk_workflow_commands_UpdateResponse(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_commands.UpdateResponse', context) : context;
    {
        const c = o.rejected;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const p = o.completed;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.completed = r;
            }));
    }
}
function walk_coresdk_workflow_commands_UpsertWorkflowSearchAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_commands.UpsertWorkflowSearchAttributes', context)
        : context;
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_coresdk_workflow_commands_WorkflowCommand(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_commands.WorkflowCommand', context) : context;
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
    {
        const a = o.eventGroupMarkers;
        if (a)
            for (const v of a)
                walk_temporal_api_sdk_v1_EventGroupMarker(v, env, ctx, pending);
    }
    {
        const c = o.scheduleActivity;
        if (c != null)
            walk_coresdk_workflow_commands_ScheduleActivity(c, env, ctx, pending);
    }
    {
        const c = o.respondToQuery;
        if (c != null)
            walk_coresdk_workflow_commands_QueryResult(c, env, ctx, pending);
    }
    {
        const c = o.completeWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_commands_CompleteWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.failWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_commands_FailWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.continueAsNewWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_commands_ContinueAsNewWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.cancelWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_commands_CancelWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.startChildWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_commands_StartChildWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.signalExternalWorkflowExecution;
        if (c != null)
            walk_coresdk_workflow_commands_SignalExternalWorkflowExecution(c, env, ctx, pending);
    }
    {
        const c = o.scheduleLocalActivity;
        if (c != null)
            walk_coresdk_workflow_commands_ScheduleLocalActivity(c, env, ctx, pending);
    }
    {
        const c = o.upsertWorkflowSearchAttributes;
        if (c != null)
            walk_coresdk_workflow_commands_UpsertWorkflowSearchAttributes(c, env, ctx, pending);
    }
    {
        const c = o.modifyWorkflowProperties;
        if (c != null)
            walk_coresdk_workflow_commands_ModifyWorkflowProperties(c, env, ctx, pending);
    }
    {
        const c = o.updateResponse;
        if (c != null)
            walk_coresdk_workflow_commands_UpdateResponse(c, env, ctx, pending);
    }
    {
        const c = o.scheduleNexusOperation;
        if (c != null)
            walk_coresdk_workflow_commands_ScheduleNexusOperation(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_completion_Failure(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_completion.Failure', context) : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_coresdk_workflow_completion_Success(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'coresdk.workflow_completion.Success', context) : context;
    {
        const a = o.commands;
        if (a)
            for (const v of a)
                walk_coresdk_workflow_commands_WorkflowCommand(v, env, ctx, pending);
    }
}
function walk_coresdk_workflow_completion_WorkflowActivationCompletion(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'coresdk.workflow_completion.WorkflowActivationCompletion', context)
        : context;
    {
        const c = o.successful;
        if (c != null)
            walk_coresdk_workflow_completion_Success(c, env, ctx, pending);
    }
    {
        const c = o.failed;
        if (c != null)
            walk_coresdk_workflow_completion_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_activity_v1_ActivityExecutionInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.activity.v1.ActivityExecutionInfo', context)
        : context;
    {
        const c = o.heartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.lastFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_activity_v1_ActivityExecutionListInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.activity.v1.ActivityExecutionListInfo', context)
        : context;
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_activity_v1_ActivityExecutionOutcome(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.activity.v1.ActivityExecutionOutcome', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_activity_v1_CallbackInfo(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.activity.v1.CallbackInfo', context) : context;
    {
        const c = o.info;
        if (c != null)
            walk_temporal_api_callback_v1_CallbackInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_batch_v1_BatchOperationReset(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.batch.v1.BatchOperationReset', context) : context;
    {
        const a = o.postResetOperations;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_PostResetOperation(v, env, ctx, pending);
    }
}
function walk_temporal_api_batch_v1_BatchOperationSignal(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.batch.v1.BatchOperationSignal', context) : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_batch_v1_BatchOperationTermination(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.batch.v1.BatchOperationTermination', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_callback_v1_CallbackInfo(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.callback.v1.CallbackInfo', context) : context;
    {
        const c = o.lastAttemptFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_CancelWorkflowExecutionCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.CancelWorkflowExecutionCommandAttributes', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_Command(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.command.v1.Command', context) : context;
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
    {
        const a = o.eventGroupMarkers;
        if (a)
            for (const v of a)
                walk_temporal_api_sdk_v1_EventGroupMarker(v, env, ctx, pending);
    }
    {
        const c = o.scheduleActivityTaskCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_ScheduleActivityTaskCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.completeWorkflowExecutionCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_CompleteWorkflowExecutionCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.failWorkflowExecutionCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_FailWorkflowExecutionCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.cancelWorkflowExecutionCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_CancelWorkflowExecutionCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.recordMarkerCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_RecordMarkerCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.continueAsNewWorkflowExecutionCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_ContinueAsNewWorkflowExecutionCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.startChildWorkflowExecutionCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_StartChildWorkflowExecutionCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.signalExternalWorkflowExecutionCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_SignalExternalWorkflowExecutionCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.upsertWorkflowSearchAttributesCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_UpsertWorkflowSearchAttributesCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.modifyWorkflowPropertiesCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_ModifyWorkflowPropertiesCommandAttributes(c, env, ctx, pending);
    }
    {
        const c = o.scheduleNexusOperationCommandAttributes;
        if (c != null)
            walk_temporal_api_command_v1_ScheduleNexusOperationCommandAttributes(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_CompleteWorkflowExecutionCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.CompleteWorkflowExecutionCommandAttributes', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_ContinueAsNewWorkflowExecutionCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.ContinueAsNewWorkflowExecutionCommandAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastCompletionResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_command_v1_FailWorkflowExecutionCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.FailWorkflowExecutionCommandAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_ModifyWorkflowPropertiesCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.ModifyWorkflowPropertiesCommandAttributes', context)
        : context;
    {
        const c = o.upsertedMemo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_RecordMarkerCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.RecordMarkerCommandAttributes', context)
        : context;
    {
        const m = o.details;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_common_v1_Payloads(v, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_ScheduleActivityTaskCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.ScheduleActivityTaskCommandAttributes', context)
        : context;
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_ScheduleNexusOperationCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.ScheduleNexusOperationCommandAttributes', context)
        : context;
    {
        const p = o.input;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.input = r;
            }));
    }
}
function walk_temporal_api_command_v1_SignalExternalWorkflowExecutionCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.SignalExternalWorkflowExecutionCommandAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_command_v1_StartChildWorkflowExecutionCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.StartChildWorkflowExecutionCommandAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_command_v1_UpsertWorkflowSearchAttributesCommandAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.command.v1.UpsertWorkflowSearchAttributesCommandAttributes', context)
        : context;
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_common_v1_Header(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.common.v1.Header', context) : context;
    {
        const m = o.fields;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_common_v1_Memo(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.common.v1.Memo', context) : context;
    {
        const m = o.fields;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_common_v1_Payloads(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.common.v1.Payloads', context) : context;
    {
        const a = o.payloads;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.payloads = r;
            }));
    }
}
function walk_temporal_api_common_v1_SearchAttributes(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.common.v1.SearchAttributes', context) : context;
    {
        const m = o.indexedFields;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_compute_v1_ComputeConfig(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.compute.v1.ComputeConfig', context) : context;
    {
        const m = o.scalingGroups;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_compute_v1_ComputeConfigScalingGroup(v, env, ctx, pending);
    }
}
function walk_temporal_api_compute_v1_ComputeConfigScalingGroup(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.compute.v1.ComputeConfigScalingGroup', context)
        : context;
    {
        const c = o.provider;
        if (c != null)
            walk_temporal_api_compute_v1_ComputeProvider(c, env, ctx, pending);
    }
    {
        const c = o.scaler;
        if (c != null)
            walk_temporal_api_compute_v1_ComputeScaler(c, env, ctx, pending);
    }
}
function walk_temporal_api_compute_v1_ComputeConfigScalingGroupUpdate(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.compute.v1.ComputeConfigScalingGroupUpdate', context)
        : context;
    {
        const c = o.scalingGroup;
        if (c != null)
            walk_temporal_api_compute_v1_ComputeConfigScalingGroup(c, env, ctx, pending);
    }
}
function walk_temporal_api_compute_v1_ComputeProvider(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.compute.v1.ComputeProvider', context) : context;
    {
        const p = o.details;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.details = r;
            }));
    }
}
function walk_temporal_api_compute_v1_ComputeScaler(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.compute.v1.ComputeScaler', context) : context;
    {
        const p = o.details;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.details = r;
            }));
    }
}
function walk_temporal_api_deployment_v1_DeploymentInfo(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.deployment.v1.DeploymentInfo', context) : context;
    {
        const m = o.metadata;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_deployment_v1_UpdateDeploymentMetadata(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.deployment.v1.UpdateDeploymentMetadata', context)
        : context;
    {
        const m = o.upsertEntries;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_deployment_v1_VersionMetadata(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.deployment.v1.VersionMetadata', context) : context;
    {
        const m = o.entries;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_deployment_v1_WorkerDeploymentVersionInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.deployment.v1.WorkerDeploymentVersionInfo', context)
        : context;
    {
        const c = o.metadata;
        if (c != null)
            walk_temporal_api_deployment_v1_VersionMetadata(c, env, ctx, pending);
    }
    {
        const c = o.computeConfig;
        if (c != null)
            walk_temporal_api_compute_v1_ComputeConfig(c, env, ctx, pending);
    }
}
function walk_temporal_api_failure_v1_ApplicationFailureInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.failure.v1.ApplicationFailureInfo', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_failure_v1_CanceledFailureInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.failure.v1.CanceledFailureInfo', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_failure_v1_Failure(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.failure.v1.Failure', context) : context;
    {
        const p = o.encodedAttributes;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.encodedAttributes = r;
            }));
    }
    {
        const c = o.cause;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.applicationFailureInfo;
        if (c != null)
            walk_temporal_api_failure_v1_ApplicationFailureInfo(c, env, ctx, pending);
    }
    {
        const c = o.timeoutFailureInfo;
        if (c != null)
            walk_temporal_api_failure_v1_TimeoutFailureInfo(c, env, ctx, pending);
    }
    {
        const c = o.canceledFailureInfo;
        if (c != null)
            walk_temporal_api_failure_v1_CanceledFailureInfo(c, env, ctx, pending);
    }
    {
        const c = o.resetWorkflowFailureInfo;
        if (c != null)
            walk_temporal_api_failure_v1_ResetWorkflowFailureInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_failure_v1_ResetWorkflowFailureInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.failure.v1.ResetWorkflowFailureInfo', context)
        : context;
    {
        const c = o.lastHeartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_failure_v1_TimeoutFailureInfo(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.failure.v1.TimeoutFailureInfo', context) : context;
    {
        const c = o.lastHeartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ActivityTaskCanceledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ActivityTaskCanceledEventAttributes', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ActivityTaskCompletedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ActivityTaskCompletedEventAttributes', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ActivityTaskFailedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ActivityTaskFailedEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ActivityTaskScheduledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ActivityTaskScheduledEventAttributes', context)
        : context;
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ActivityTaskStartedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ActivityTaskStartedEventAttributes', context)
        : context;
    {
        const c = o.lastFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ActivityTaskTimedOutEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ActivityTaskTimedOutEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ChildWorkflowExecutionCanceledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ChildWorkflowExecutionCanceledEventAttributes', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ChildWorkflowExecutionCompletedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ChildWorkflowExecutionCompletedEventAttributes', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ChildWorkflowExecutionFailedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ChildWorkflowExecutionFailedEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_ChildWorkflowExecutionStartedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.ChildWorkflowExecutionStartedEventAttributes', context)
        : context;
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_History(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.history.v1.History', context) : context;
    {
        const a = o.events;
        if (a)
            for (const v of a)
                walk_temporal_api_history_v1_HistoryEvent(v, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_HistoryEvent(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.history.v1.HistoryEvent', context) : context;
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
    {
        const a = o.eventGroupMarkers;
        if (a)
            for (const v of a)
                walk_temporal_api_sdk_v1_EventGroupMarker(v, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionStartedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionStartedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionCompletedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionCompletedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionFailedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionFailedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowTaskFailedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowTaskFailedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.activityTaskScheduledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ActivityTaskScheduledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.activityTaskStartedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ActivityTaskStartedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.activityTaskCompletedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ActivityTaskCompletedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.activityTaskFailedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ActivityTaskFailedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.activityTaskTimedOutEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ActivityTaskTimedOutEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.activityTaskCanceledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ActivityTaskCanceledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.markerRecordedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_MarkerRecordedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionSignaledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionSignaledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionTerminatedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionTerminatedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionCanceledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionCanceledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionContinuedAsNewEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionContinuedAsNewEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.startChildWorkflowExecutionInitiatedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_StartChildWorkflowExecutionInitiatedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.childWorkflowExecutionStartedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ChildWorkflowExecutionStartedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.childWorkflowExecutionCompletedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ChildWorkflowExecutionCompletedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.childWorkflowExecutionFailedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ChildWorkflowExecutionFailedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.childWorkflowExecutionCanceledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_ChildWorkflowExecutionCanceledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.signalExternalWorkflowExecutionInitiatedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_SignalExternalWorkflowExecutionInitiatedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.upsertWorkflowSearchAttributesEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_UpsertWorkflowSearchAttributesEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionUpdateAcceptedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionUpdateAcceptedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionUpdateRejectedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionUpdateRejectedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionUpdateCompletedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionUpdateCompletedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowPropertiesModifiedExternallyEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowPropertiesModifiedExternallyEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowPropertiesModifiedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowPropertiesModifiedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionUpdateAdmittedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_WorkflowExecutionUpdateAdmittedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.nexusOperationScheduledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_NexusOperationScheduledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.nexusOperationCompletedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_NexusOperationCompletedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.nexusOperationFailedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_NexusOperationFailedEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.nexusOperationCanceledEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_NexusOperationCanceledEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.nexusOperationTimedOutEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_NexusOperationTimedOutEventAttributes(c, env, ctx, pending);
    }
    {
        const c = o.nexusOperationCancelRequestFailedEventAttributes;
        if (c != null)
            walk_temporal_api_history_v1_NexusOperationCancelRequestFailedEventAttributes(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_MarkerRecordedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.MarkerRecordedEventAttributes', context)
        : context;
    {
        const m = o.details;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_common_v1_Payloads(v, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_NexusOperationCanceledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.NexusOperationCanceledEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_NexusOperationCancelRequestFailedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.NexusOperationCancelRequestFailedEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_NexusOperationCompletedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.NexusOperationCompletedEventAttributes', context)
        : context;
    {
        const p = o.result;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.result = r;
            }));
    }
}
function walk_temporal_api_history_v1_NexusOperationFailedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.NexusOperationFailedEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_NexusOperationScheduledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.NexusOperationScheduledEventAttributes', context)
        : context;
    {
        const p = o.input;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.input = r;
            }));
    }
}
function walk_temporal_api_history_v1_NexusOperationTimedOutEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.NexusOperationTimedOutEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_SignalExternalWorkflowExecutionInitiatedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.SignalExternalWorkflowExecutionInitiatedEventAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_StartChildWorkflowExecutionInitiatedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.StartChildWorkflowExecutionInitiatedEventAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_history_v1_UpsertWorkflowSearchAttributesEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.UpsertWorkflowSearchAttributesEventAttributes', context)
        : context;
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionCanceledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionCanceledEventAttributes', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionCompletedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionCompletedEventAttributes', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionContinuedAsNewEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionContinuedAsNewEventAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastCompletionResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionFailedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionFailedEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionSignaledEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionSignaledEventAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionStartedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionStartedEventAttributes', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.continuedFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastCompletionResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionTerminatedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionTerminatedEventAttributes', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionUpdateAcceptedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionUpdateAcceptedEventAttributes', context)
        : context;
    {
        const c = o.acceptedRequest;
        if (c != null)
            walk_temporal_api_update_v1_Request(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionUpdateAdmittedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionUpdateAdmittedEventAttributes', context)
        : context;
    {
        const c = o.request;
        if (c != null)
            walk_temporal_api_update_v1_Request(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionUpdateCompletedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionUpdateCompletedEventAttributes', context)
        : context;
    {
        const c = o.outcome;
        if (c != null)
            walk_temporal_api_update_v1_Outcome(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowExecutionUpdateRejectedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowExecutionUpdateRejectedEventAttributes', context)
        : context;
    {
        const c = o.rejectedRequest;
        if (c != null)
            walk_temporal_api_update_v1_Request(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowPropertiesModifiedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowPropertiesModifiedEventAttributes', context)
        : context;
    {
        const c = o.upsertedMemo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowPropertiesModifiedExternallyEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowPropertiesModifiedExternallyEventAttributes', context)
        : context;
    {
        const c = o.upsertedMemo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
}
function walk_temporal_api_history_v1_WorkflowTaskFailedEventAttributes(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.history.v1.WorkflowTaskFailedEventAttributes', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_nexus_v1_NexusOperationExecutionCancellationInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.nexus.v1.NexusOperationExecutionCancellationInfo', context)
        : context;
    {
        const c = o.lastAttemptFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_nexus_v1_NexusOperationExecutionInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.nexus.v1.NexusOperationExecutionInfo', context)
        : context;
    {
        const c = o.lastAttemptFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.cancellationInfo;
        if (c != null)
            walk_temporal_api_nexus_v1_NexusOperationExecutionCancellationInfo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_nexus_v1_NexusOperationExecutionListInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.nexus.v1.NexusOperationExecutionListInfo', context)
        : context;
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_nexus_v1_Request(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.nexus.v1.Request', context) : context;
    {
        const c = o.startOperation;
        if (c != null)
            walk_temporal_api_nexus_v1_StartOperationRequest(c, env, ctx, pending);
    }
}
function walk_temporal_api_nexus_v1_Response(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.nexus.v1.Response', context) : context;
    {
        const c = o.startOperation;
        if (c != null)
            walk_temporal_api_nexus_v1_StartOperationResponse(c, env, ctx, pending);
    }
}
function walk_temporal_api_nexus_v1_StartOperationRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.nexus.v1.StartOperationRequest', context)
        : context;
    {
        const p = o.payload;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.payload = r;
            }));
    }
}
function walk_temporal_api_nexus_v1_StartOperationResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.nexus.v1.StartOperationResponse', context)
        : context;
    {
        const c = o.syncSuccess;
        if (c != null)
            walk_temporal_api_nexus_v1_StartOperationResponse_Sync(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_nexus_v1_StartOperationResponse_Sync(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.nexus.v1.StartOperationResponse.Sync', context)
        : context;
    {
        const p = o.payload;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.payload = r;
            }));
    }
}
function walk_temporal_api_query_v1_WorkflowQuery(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.query.v1.WorkflowQuery', context) : context;
    {
        const c = o.queryArgs;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_query_v1_WorkflowQueryResult(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.query.v1.WorkflowQueryResult', context) : context;
    {
        const c = o.answer;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_schedule_v1_Schedule(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.schedule.v1.Schedule', context) : context;
    {
        const c = o.action;
        if (c != null)
            walk_temporal_api_schedule_v1_ScheduleAction(c, env, ctx, pending);
    }
}
function walk_temporal_api_schedule_v1_ScheduleAction(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.schedule.v1.ScheduleAction', context) : context;
    {
        const c = o.startWorkflow;
        if (c != null)
            walk_temporal_api_workflow_v1_NewWorkflowExecutionInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_schedule_v1_ScheduleListEntry(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.schedule.v1.ScheduleListEntry', context) : context;
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_sdk_v1_EventGroupMarker(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.sdk.v1.EventGroupMarker', context) : context;
    {
        const c = o.label;
        if (c != null)
            walk_temporal_api_sdk_v1_EventGroupMarker_Label(c, env, ctx, pending);
    }
}
function walk_temporal_api_sdk_v1_EventGroupMarker_Label(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.sdk.v1.EventGroupMarker.Label', context) : context;
    {
        const p = o.label;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.label = r;
            }));
    }
}
function walk_temporal_api_sdk_v1_UserMetadata(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.sdk.v1.UserMetadata', context) : context;
    {
        const p = o.summary;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.summary = r;
            }));
    }
    {
        const p = o.details;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.details = r;
            }));
    }
}
function walk_temporal_api_update_v1_Input(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.update.v1.Input', context) : context;
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.args;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_update_v1_Outcome(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.update.v1.Outcome', context) : context;
    {
        const c = o.success;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_update_v1_Request(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.update.v1.Request', context) : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_update_v1_Input(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_CallbackInfo(o, env, context, pending) {
    const ctx = env.deriveContext ? env.deriveContext(o, 'temporal.api.workflow.v1.CallbackInfo', context) : context;
    {
        const c = o.lastAttemptFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_NewWorkflowExecutionInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.NewWorkflowExecutionInfo', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_NexusOperationCancellationInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.NexusOperationCancellationInfo', context)
        : context;
    {
        const c = o.lastAttemptFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_PendingActivityInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.PendingActivityInfo', context)
        : context;
    {
        const c = o.heartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.lastFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_PendingNexusOperationInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.PendingNexusOperationInfo', context)
        : context;
    {
        const c = o.lastAttemptFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.cancellationInfo;
        if (c != null)
            walk_temporal_api_workflow_v1_NexusOperationCancellationInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_PostResetOperation(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.PostResetOperation', context)
        : context;
    {
        const c = o.signalWorkflow;
        if (c != null)
            walk_temporal_api_workflow_v1_PostResetOperation_SignalWorkflow(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_PostResetOperation_SignalWorkflow(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.PostResetOperation.SignalWorkflow', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_WorkflowExecutionConfig(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.WorkflowExecutionConfig', context)
        : context;
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflow_v1_WorkflowExecutionInfo(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflow.v1.WorkflowExecutionInfo', context)
        : context;
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_workflowservice_v1_CountActivityExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountActivityExecutionsResponse', context)
        : context;
    {
        const a = o.groups;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_CountActivityExecutionsResponse_AggregationGroup(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_CountActivityExecutionsResponse_AggregationGroup(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountActivityExecutionsResponse.AggregationGroup', context)
        : context;
    {
        const a = o.groupValues;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.groupValues = r;
            }));
    }
}
function walk_temporal_api_workflowservice_v1_CountNexusOperationExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountNexusOperationExecutionsResponse', context)
        : context;
    {
        const a = o.groups;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_CountNexusOperationExecutionsResponse_AggregationGroup(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_CountNexusOperationExecutionsResponse_AggregationGroup(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountNexusOperationExecutionsResponse.AggregationGroup', context)
        : context;
    {
        const a = o.groupValues;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.groupValues = r;
            }));
    }
}
function walk_temporal_api_workflowservice_v1_CountSchedulesResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountSchedulesResponse', context)
        : context;
    {
        const a = o.groups;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_CountSchedulesResponse_AggregationGroup(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_CountSchedulesResponse_AggregationGroup(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountSchedulesResponse.AggregationGroup', context)
        : context;
    {
        const a = o.groupValues;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.groupValues = r;
            }));
    }
}
function walk_temporal_api_workflowservice_v1_CountWorkflowExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountWorkflowExecutionsResponse', context)
        : context;
    {
        const a = o.groups;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_CountWorkflowExecutionsResponse_AggregationGroup(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_CountWorkflowExecutionsResponse_AggregationGroup(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CountWorkflowExecutionsResponse.AggregationGroup', context)
        : context;
    {
        const a = o.groupValues;
        if (a && a.length)
            pending.push(env.transformPayloads(a, ctx).then((r) => {
                o.groupValues = r;
            }));
    }
}
function walk_temporal_api_workflowservice_v1_CreateScheduleRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CreateScheduleRequest', context)
        : context;
    {
        const c = o.schedule;
        if (c != null)
            walk_temporal_api_schedule_v1_Schedule(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_workflowservice_v1_CreateWorkerDeploymentVersionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.CreateWorkerDeploymentVersionRequest', context)
        : context;
    {
        const c = o.computeConfig;
        if (c != null)
            walk_temporal_api_compute_v1_ComputeConfig(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_DescribeActivityExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.DescribeActivityExecutionResponse', context)
        : context;
    {
        const c = o.info;
        if (c != null)
            walk_temporal_api_activity_v1_ActivityExecutionInfo(c, env, ctx, pending);
    }
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.outcome;
        if (c != null)
            walk_temporal_api_activity_v1_ActivityExecutionOutcome(c, env, ctx, pending);
    }
    {
        const a = o.callbacks;
        if (a)
            for (const v of a)
                walk_temporal_api_activity_v1_CallbackInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_DescribeDeploymentResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.DescribeDeploymentResponse', context)
        : context;
    {
        const c = o.deploymentInfo;
        if (c != null)
            walk_temporal_api_deployment_v1_DeploymentInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_DescribeNexusOperationExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.DescribeNexusOperationExecutionResponse', context)
        : context;
    {
        const c = o.info;
        if (c != null)
            walk_temporal_api_nexus_v1_NexusOperationExecutionInfo(c, env, ctx, pending);
    }
    {
        const p = o.input;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.input = r;
            }));
    }
    {
        const p = o.result;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.result = r;
            }));
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_DescribeScheduleResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.DescribeScheduleResponse', context)
        : context;
    {
        const c = o.schedule;
        if (c != null)
            walk_temporal_api_schedule_v1_Schedule(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
}
function walk_temporal_api_workflowservice_v1_DescribeWorkerDeploymentVersionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.DescribeWorkerDeploymentVersionResponse', context)
        : context;
    {
        const c = o.workerDeploymentVersionInfo;
        if (c != null)
            walk_temporal_api_deployment_v1_WorkerDeploymentVersionInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_DescribeWorkflowExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.DescribeWorkflowExecutionResponse', context)
        : context;
    {
        const c = o.executionConfig;
        if (c != null)
            walk_temporal_api_workflow_v1_WorkflowExecutionConfig(c, env, ctx, pending);
    }
    {
        const c = o.workflowExecutionInfo;
        if (c != null)
            walk_temporal_api_workflow_v1_WorkflowExecutionInfo(c, env, ctx, pending);
    }
    {
        const a = o.pendingActivities;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_PendingActivityInfo(v, env, ctx, pending);
    }
    {
        const a = o.callbacks;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_CallbackInfo(v, env, ctx, pending);
    }
    {
        const a = o.pendingNexusOperations;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_PendingNexusOperationInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ExecuteMultiOperationRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ExecuteMultiOperationRequest', context)
        : context;
    {
        const a = o.operations;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_ExecuteMultiOperationRequest_Operation(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ExecuteMultiOperationRequest_Operation(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ExecuteMultiOperationRequest.Operation', context)
        : context;
    {
        const c = o.startWorkflow;
        if (c != null)
            walk_temporal_api_workflowservice_v1_StartWorkflowExecutionRequest(c, env, ctx, pending);
    }
    {
        const c = o.updateWorkflow;
        if (c != null)
            walk_temporal_api_workflowservice_v1_UpdateWorkflowExecutionRequest(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ExecuteMultiOperationResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ExecuteMultiOperationResponse', context)
        : context;
    {
        const a = o.responses;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_ExecuteMultiOperationResponse_Response(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ExecuteMultiOperationResponse_Response(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ExecuteMultiOperationResponse.Response', context)
        : context;
    {
        const c = o.startWorkflow;
        if (c != null)
            walk_temporal_api_workflowservice_v1_StartWorkflowExecutionResponse(c, env, ctx, pending);
    }
    {
        const c = o.updateWorkflow;
        if (c != null)
            walk_temporal_api_workflowservice_v1_UpdateWorkflowExecutionResponse(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_GetCurrentDeploymentResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.GetCurrentDeploymentResponse', context)
        : context;
    {
        const c = o.currentDeploymentInfo;
        if (c != null)
            walk_temporal_api_deployment_v1_DeploymentInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_GetDeploymentReachabilityResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.GetDeploymentReachabilityResponse', context)
        : context;
    {
        const c = o.deploymentInfo;
        if (c != null)
            walk_temporal_api_deployment_v1_DeploymentInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_GetWorkflowExecutionHistoryResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.GetWorkflowExecutionHistoryResponse', context)
        : context;
    {
        const c = o.history;
        if (c != null)
            walk_temporal_api_history_v1_History(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_GetWorkflowExecutionHistoryReverseResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.GetWorkflowExecutionHistoryReverseResponse', context)
        : context;
    {
        const c = o.history;
        if (c != null)
            walk_temporal_api_history_v1_History(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListActivityExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListActivityExecutionsResponse', context)
        : context;
    {
        const a = o.executions;
        if (a)
            for (const v of a)
                walk_temporal_api_activity_v1_ActivityExecutionListInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListArchivedWorkflowExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListArchivedWorkflowExecutionsResponse', context)
        : context;
    {
        const a = o.executions;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_WorkflowExecutionInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListClosedWorkflowExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListClosedWorkflowExecutionsResponse', context)
        : context;
    {
        const a = o.executions;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_WorkflowExecutionInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListNexusOperationExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListNexusOperationExecutionsResponse', context)
        : context;
    {
        const a = o.operations;
        if (a)
            for (const v of a)
                walk_temporal_api_nexus_v1_NexusOperationExecutionListInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListOpenWorkflowExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListOpenWorkflowExecutionsResponse', context)
        : context;
    {
        const a = o.executions;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_WorkflowExecutionInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListSchedulesResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListSchedulesResponse', context)
        : context;
    {
        const a = o.schedules;
        if (a)
            for (const v of a)
                walk_temporal_api_schedule_v1_ScheduleListEntry(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ListWorkflowExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ListWorkflowExecutionsResponse', context)
        : context;
    {
        const a = o.executions;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_WorkflowExecutionInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_PollActivityExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.PollActivityExecutionResponse', context)
        : context;
    {
        const c = o.outcome;
        if (c != null)
            walk_temporal_api_activity_v1_ActivityExecutionOutcome(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_PollActivityTaskQueueResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.PollActivityTaskQueueResponse', context)
        : context;
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.heartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_PollNexusOperationExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.PollNexusOperationExecutionResponse', context)
        : context;
    {
        const p = o.result;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.result = r;
            }));
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_PollNexusTaskQueueResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.PollNexusTaskQueueResponse', context)
        : context;
    {
        const c = o.request;
        if (c != null)
            walk_temporal_api_nexus_v1_Request(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_PollWorkflowExecutionUpdateResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.PollWorkflowExecutionUpdateResponse', context)
        : context;
    {
        const c = o.outcome;
        if (c != null)
            walk_temporal_api_update_v1_Outcome(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_PollWorkflowTaskQueueResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.PollWorkflowTaskQueueResponse', context)
        : context;
    {
        const c = o.history;
        if (c != null)
            walk_temporal_api_history_v1_History(c, env, ctx, pending);
    }
    {
        const c = o.query;
        if (c != null)
            walk_temporal_api_query_v1_WorkflowQuery(c, env, ctx, pending);
    }
    {
        const m = o.queries;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_query_v1_WorkflowQuery(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_QueryWorkflowRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.QueryWorkflowRequest', context)
        : context;
    {
        const c = o.query;
        if (c != null)
            walk_temporal_api_query_v1_WorkflowQuery(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_QueryWorkflowResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.QueryWorkflowResponse', context)
        : context;
    {
        const c = o.queryResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RecordActivityTaskHeartbeatByIdRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RecordActivityTaskHeartbeatByIdRequest', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RecordActivityTaskHeartbeatRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RecordActivityTaskHeartbeatRequest', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ResetWorkflowExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ResetWorkflowExecutionRequest', context)
        : context;
    {
        const a = o.postResetOperations;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_PostResetOperation(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskCanceledByIdRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskCanceledByIdRequest', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskCanceledRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskCanceledRequest', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskCompletedByIdRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskCompletedByIdRequest', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskCompletedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskCompletedRequest', context)
        : context;
    {
        const c = o.result;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedByIdRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskFailedByIdRequest', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastHeartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedByIdResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskFailedByIdResponse', context)
        : context;
    {
        const a = o.failures;
        if (a)
            for (const v of a)
                walk_temporal_api_failure_v1_Failure(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskFailedRequest', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastHeartbeatDetails;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondActivityTaskFailedResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondActivityTaskFailedResponse', context)
        : context;
    {
        const a = o.failures;
        if (a)
            for (const v of a)
                walk_temporal_api_failure_v1_Failure(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondNexusTaskCompletedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondNexusTaskCompletedRequest', context)
        : context;
    {
        const c = o.response;
        if (c != null)
            walk_temporal_api_nexus_v1_Response(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondNexusTaskFailedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondNexusTaskFailedRequest', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondQueryTaskCompletedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondQueryTaskCompletedRequest', context)
        : context;
    {
        const c = o.queryResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondWorkflowTaskCompletedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondWorkflowTaskCompletedRequest', context)
        : context;
    {
        const a = o.commands;
        if (a)
            for (const v of a)
                walk_temporal_api_command_v1_Command(v, env, ctx, pending);
    }
    {
        const m = o.queryResults;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_query_v1_WorkflowQueryResult(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondWorkflowTaskCompletedResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondWorkflowTaskCompletedResponse', context)
        : context;
    {
        const c = o.workflowTask;
        if (c != null)
            walk_temporal_api_workflowservice_v1_PollWorkflowTaskQueueResponse(c, env, ctx, pending);
    }
    {
        const a = o.activityTasks;
        if (a)
            for (const v of a)
                walk_temporal_api_workflowservice_v1_PollActivityTaskQueueResponse(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_RespondWorkflowTaskFailedRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.RespondWorkflowTaskFailedRequest', context)
        : context;
    {
        const c = o.failure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ScanWorkflowExecutionsResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ScanWorkflowExecutionsResponse', context)
        : context;
    {
        const a = o.executions;
        if (a)
            for (const v of a)
                walk_temporal_api_workflow_v1_WorkflowExecutionInfo(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_SetCurrentDeploymentRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.SetCurrentDeploymentRequest', context)
        : context;
    {
        const c = o.updateMetadata;
        if (c != null)
            walk_temporal_api_deployment_v1_UpdateDeploymentMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_SetCurrentDeploymentResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.SetCurrentDeploymentResponse', context)
        : context;
    {
        const c = o.currentDeploymentInfo;
        if (c != null)
            walk_temporal_api_deployment_v1_DeploymentInfo(c, env, ctx, pending);
    }
    {
        const c = o.previousDeploymentInfo;
        if (c != null)
            walk_temporal_api_deployment_v1_DeploymentInfo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_SignalWithStartWorkflowExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.SignalWithStartWorkflowExecutionRequest', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.signalInput;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_SignalWorkflowExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.SignalWorkflowExecutionRequest', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_StartActivityExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.StartActivityExecutionRequest', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_StartBatchOperationRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.StartBatchOperationRequest', context)
        : context;
    {
        const c = o.terminationOperation;
        if (c != null)
            walk_temporal_api_batch_v1_BatchOperationTermination(c, env, ctx, pending);
    }
    {
        const c = o.signalOperation;
        if (c != null)
            walk_temporal_api_batch_v1_BatchOperationSignal(c, env, ctx, pending);
    }
    {
        const c = o.resetOperation;
        if (c != null)
            walk_temporal_api_batch_v1_BatchOperationReset(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_StartNexusOperationExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.StartNexusOperationExecutionRequest', context)
        : context;
    {
        const p = o.input;
        if (p != null)
            pending.push(env.transformPayload(p, ctx).then((r) => {
                o.input = r;
            }));
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_StartWorkflowExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.StartWorkflowExecutionRequest', context)
        : context;
    {
        const c = o.input;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.header;
        if (c != null)
            walk_temporal_api_common_v1_Header(c, env, ctx, pending);
    }
    {
        const c = o.continuedFailure;
        if (c != null)
            walk_temporal_api_failure_v1_Failure(c, env, ctx, pending);
    }
    {
        const c = o.lastCompletionResult;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
    {
        const c = o.userMetadata;
        if (c != null)
            walk_temporal_api_sdk_v1_UserMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_StartWorkflowExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.StartWorkflowExecutionResponse', context)
        : context;
    {
        const c = o.eagerWorkflowTask;
        if (c != null)
            walk_temporal_api_workflowservice_v1_PollWorkflowTaskQueueResponse(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_TerminateWorkflowExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.TerminateWorkflowExecutionRequest', context)
        : context;
    {
        const c = o.details;
        if (c != null)
            walk_temporal_api_common_v1_Payloads(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_UpdateScheduleRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.UpdateScheduleRequest', context)
        : context;
    {
        const c = o.schedule;
        if (c != null)
            walk_temporal_api_schedule_v1_Schedule(c, env, ctx, pending);
    }
    {
        if (!env.skipSearchAttributes) {
            const c = o.searchAttributes;
            if (c != null)
                walk_temporal_api_common_v1_SearchAttributes(c, env, ctx, pending);
        }
    }
    {
        const c = o.memo;
        if (c != null)
            walk_temporal_api_common_v1_Memo(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_UpdateWorkerDeploymentVersionComputeConfigRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.UpdateWorkerDeploymentVersionComputeConfigRequest', context)
        : context;
    {
        const m = o.computeConfigScalingGroups;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_compute_v1_ComputeConfigScalingGroupUpdate(v, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_UpdateWorkerDeploymentVersionMetadataRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.UpdateWorkerDeploymentVersionMetadataRequest', context)
        : context;
    {
        const m = o.upsertEntries;
        if (m)
            for (const [k, v] of Object.entries(m))
                pending.push(env.transformPayload(v, ctx).then((r) => {
                    m[k] = r;
                }));
    }
}
function walk_temporal_api_workflowservice_v1_UpdateWorkerDeploymentVersionMetadataResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.UpdateWorkerDeploymentVersionMetadataResponse', context)
        : context;
    {
        const c = o.metadata;
        if (c != null)
            walk_temporal_api_deployment_v1_VersionMetadata(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_UpdateWorkflowExecutionRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.UpdateWorkflowExecutionRequest', context)
        : context;
    {
        const c = o.request;
        if (c != null)
            walk_temporal_api_update_v1_Request(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_UpdateWorkflowExecutionResponse(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.UpdateWorkflowExecutionResponse', context)
        : context;
    {
        const c = o.outcome;
        if (c != null)
            walk_temporal_api_update_v1_Outcome(c, env, ctx, pending);
    }
}
function walk_temporal_api_workflowservice_v1_ValidateWorkerDeploymentVersionComputeConfigRequest(o, env, context, pending) {
    const ctx = env.deriveContext
        ? env.deriveContext(o, 'temporal.api.workflowservice.v1.ValidateWorkerDeploymentVersionComputeConfigRequest', context)
        : context;
    {
        const m = o.computeConfigScalingGroups;
        if (m)
            for (const v of Object.values(m))
                walk_temporal_api_compute_v1_ComputeConfigScalingGroupUpdate(v, env, ctx, pending);
    }
}
//# sourceMappingURL=payload-visitor.generated.js.map