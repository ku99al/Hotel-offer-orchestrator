"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.InternalActivityStartOptionsSymbol = exports.InternalWorkflowQueryOptionsSymbol = exports.InternalWorkflowSignalOptionsSymbol = exports.InternalWorkflowUpdateOptionsSymbol = exports.InternalWorkflowStartOptionsSymbol = void 0;
/**
 * A symbol used to attach extra, SDK-internal options to the `WorkflowClient.start()` call.
 *
 * These are notably used by the Temporal Nexus helpers.
 *
 * @internal
 * @hidden
 */
exports.InternalWorkflowStartOptionsSymbol = Symbol.for('__temporal_internal_client_workflow_start_options');
/**
 * A symbol used to attach extra, SDK-internal options to a `WorkflowHandle.startUpdate()` /
 * `executeUpdate()` call.
 *
 * Used by the Temporal Nexus helpers to attach a completion callback, request ID, and request links
 * onto the UpdateWorkflowExecution request, and to capture the response link the server returns.
 *
 * @internal
 * @hidden
 */
exports.InternalWorkflowUpdateOptionsSymbol = Symbol.for('__temporal_internal_client_workflow_update_options');
/**
 * A symbol used to attach extra, SDK-internal options to a `WorkflowHandle.signal()` call.
 *
 * Used by the Temporal Nexus helpers to forward request links onto the signal request
 * and to capture the response link returned on the SignalWorkflowExecutionResponse.
 *
 * @internal
 * @hidden
 */
exports.InternalWorkflowSignalOptionsSymbol = Symbol.for('__temporal_internal_client_workflow_signal_options');
/**
 * A symbol used to attach Nexus-specific options to a `WorkflowHandle.query()` call, so the client
 * can capture the response link returned on the QueryWorkflowResponse.
 *
 * Unlike the signal variant there are no request links: a Query writes nothing to history, so
 * `QueryWorkflowRequest` has no `links` field and there is no event to link from.
 *
 * @internal
 * @hidden
 */
exports.InternalWorkflowQueryOptionsSymbol = Symbol.for('__temporal_internal_client_workflow_query_options');
/**
 * A symbol used to attach Nexus-specific options to an `ActivityClient.start()` call.
 *
 * @internal
 * @hidden
 */
exports.InternalActivityStartOptionsSymbol = Symbol.for('__temporal_internal_client_activity_start_options');
//# sourceMappingURL=internal.js.map