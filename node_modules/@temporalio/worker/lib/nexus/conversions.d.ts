import * as nexus from 'nexus-rpc';
import type { LoadedDataConverter, ProtoFailure, TypeInfo } from '@temporalio/common';
import type { temporal } from '@temporalio/proto';
/**
 * Error type used by a Payload Converter or Payload Codec to report that a Payload failed
 * validation, i.e. that the input is invalid rather than that the handler failed to process it.
 *
 * When a Nexus operation's input fails to decode with a non-retryable {@link ApplicationFailure} of
 * this type, the resulting Nexus Handler Error is `BAD_REQUEST` rather than `INTERNAL`.
 */
export declare const PAYLOAD_VALIDATION_ERROR_TYPE = "PayloadValidationError";
/** Decode Payload Codecs and apply optional TypeInfo while translating invalid Nexus input errors. */
export declare function decodePayload(dataConverter: LoadedDataConverter, payload: temporal.api.common.v1.IPayload | undefined, typeInfo?: TypeInfo): Promise<unknown>;
export declare function operationErrorToProto(dataConverter: LoadedDataConverter, err: nexus.OperationError): Promise<ProtoFailure>;
export declare function handlerErrorToProto(dataConverter: LoadedDataConverter, err: nexus.HandlerError): Promise<ProtoFailure>;
export declare function coerceToHandlerError(err: unknown): nexus.HandlerError;
